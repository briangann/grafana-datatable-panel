import { DataFrame } from '@grafana/data';
import { TransformationOptions } from 'types';

export function transformData(DataFrames: DataFrame[], transformation: TransformationOptions): DataFrame[] {
  return DataFrames;
}
